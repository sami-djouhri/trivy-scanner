name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # shellcheck ist auf ubuntu-latest vorinstalliert
      - name: ShellCheck
        run: shellcheck scan.sh
